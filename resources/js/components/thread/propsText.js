export default {
    bodyText: {
        type: String,
        default: 'content'
    },
    titleText: {
        type: String,
        default: 'title'
    },
    submitButtonText: {
        type: String,
        default: 'send'
    }
}
